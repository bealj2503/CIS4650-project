int main(int ret) {
  
  int x; int fac;
  
  x = input();
  ret = x;
  
  fac = 1;

  while (x > 1) {
    fac = fac * ret;
    x = x - 1;
  } 
  if(ret != 0){
    fac = fac + main(ret);
  }
  output(fac);
  return fac;
}

